name: Deploy to Amazon ECS

on:
  push:
    branches: ["master"]

env:
  AWS_REGION: ca-central-1
  ECR_REPOSITORY: coolify-testing
  ECS_SERVICE: coolify-test-service-jw27zlew
  ECS_CLUSTER: coolify-cluster
  ECS_TASK_DEFINITION: task-definition.json
  CONTAINER_NAME: coolify-test-container

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get env from Parameter Store
        run: |
          ENV_VALUE=$(aws ssm get-parameter \
            --name "/vuugo/preview/test-env" \
            --with-decryption \
            --query "Parameter.Value" \
            --output text)
          echo "NEXT_PUBLIC_TEST_TITLE=$(echo "$ENV_VALUE" | grep NEXT_PUBLIC_TEST_TITLE | cut -d'=' -f2-)" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_TEST_DES=$(echo "$ENV_VALUE" | grep NEXT_PUBLIC_TEST_DES | cut -d'=' -f2-)" >> $GITHUB_ENV

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_TEST_TITLE="$NEXT_PUBLIC_TEST_TITLE" \
            --build-arg NEXT_PUBLIC_TEST_DES="$NEXT_PUBLIC_TEST_DES" \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Fill in the new image ID in the Amazon ECS task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: ${{ env.ECS_TASK_DEFINITION }}
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy Amazon ECS task definition
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true
